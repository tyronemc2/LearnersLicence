{
  "name": "learners-licence-practice-platform",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "devDependencies": {
    "typescript": "^5.8.3"
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.app.json && cp index.html dist/index.html && cp -R public dist/public && cp src/styles.css dist/styles.css",
    "dev": "npm run build && python3 -m http.server 4173 -d dist",
    "test": "npm run build && node --test test/*.test.mjs"
  }
}
